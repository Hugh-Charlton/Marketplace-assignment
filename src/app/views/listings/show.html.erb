<%#= navigational links %>
<%= link_to "Home", "/pages/home" %>
<%= link_to "Marketplace", "/listings/index" %>
<%= link_to "Contact Us", "/pages/Contact" %>

<%#= Uses variable to show the title of listing in header %>
<h1><%= @listing.title %><h1>

<p><%= listing.description %></p>
<p><%= listing.cost %></p>

<br>

<p><%= link_to "edit", edit_listing_path(listing.id) %></p>

<%#= link to destroy record from database with a confirmation message %>
<p><%= link_to 'Destroy', article_path(article), method: :delete, data: { confirm: 'Are you sure?' } %></p>